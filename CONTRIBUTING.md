# 贡献指南

## 提交前检查

- 不要提交 `Library/`、`Logs/`、`UserSettings/` 或本地构建缓存。
- Unity 版本优先使用 `6000.0.62f1`。
- 新增玩法时，同时更新 `README.md` 与 `TESTING.md`。
- 对核心规则增加可重复的验证步骤。

## 分支命名

- `feature/<name>`：新功能
- `fix/<name>`：问题修复
- `docs/<name>`：文档和交付改进

## Pull Request

请说明目标项目、用户可见变化、测试命令和已知限制。涉及三个子项目的共享框架修改时，请确认同步脚本执行后三个项目都能通过验证。
