# 验证记录与试玩清单

## 本机验证（2026-09-05）

### 已通过

运行 `D:\github\NeonBreach\Tools\Test-Core.ps1`，**115 项断言 / 源码检查通过**：

- 用 PowerShell 7 内置 Roslyn 实际编译无 Unity 依赖的 `CombatRules.cs` 与 `GridPathfinder.cs`。
- 实际执行射击、空仓、换弹计时、换弹期间禁射、部分备弹转移、弹药上限与非法输入检查。
- 实际执行波次规模、生命值增长、分数和生成间隔边界检查。
- 实际执行 A* 最短路径、绕墙、连续路径、无对角穿角、不可达目标、越界、目标投影和全封闭网格检查。
- 重建地图掩体占用网格，检查五个敌人出生点到玩家起点都存在路径。
- 对全部 13 个 C# 源文件的 Player 和 `UNITY_EDITOR` 两种预处理分支做 Roslyn 语法解析。
- 检查包清单、场景启动脚本 GUID、资产 GUID 唯一性与 PowerShell 启动脚本语法。
- 启动脚本的“未安装 Unity Editor”分支已执行：给出安装提示，并以错误码 1 退出。

### 未执行 / 不能据此声称通过

本机常见路径和 Unity Hub 记录中未找到 Editor，也没有可用的 Unity 引用程序集：

- Editor Play Mode、碰撞与输入验证。
- 实际渲染截图、UI 排版与声音试听。
- 五波流程的端到端游玩、难度平衡和性能实测。

已完成：Unity API/类型编译、Unity 工程导入、项目验证、Windows 独立构建。

因此，“115 项通过”**不是** 115 项 Unity 集成测试，更不意味着游戏已完成实机验收。

## 安装 Unity 后的检查步骤

先运行 Unity 菜单 **Neon Breach → Validate Project**。通过后依次执行以下项目。

| 检查 | 预期结果 |
| --- | --- |
| 导入工程 | Console 无脚本编译错误，Arena 场景启动组件没有 Missing Script |
| 首次 Play | 生成竞技场、出现 NEON BREACH 标题及 DEPLOY 按钮 |
| 部署 | 鼠标锁定，约 2.5 秒准备后进入第一波 |
| 基础移动 | WASD、视角、跳跃正常，无法穿越围墙和装甲掩体 |
| 冲刺 | 左 Shift + W 加速，消耗耐力；耗尽后停止冲刺并恢复 |
| 射击 | 长按左键连续开火，命中机器人减少血量，命中墙体出现火花 |
| 瞄准 | 按住右键视野收窄、枪械居中、散布减小 |
| 弱点 | 击中头部显示 CRITICAL，伤害及击杀得分高于普通命中 |
| 换弹 | R 开始装弹，装弹期间不发射，完成后正确扣除备用弹药 |
| 敌人导航 | 敌人从五个平台进入，能绕过掩体，不持续卡墙 |
| 敌人攻击 | 面罩变白后开火；玩家移动可避开预先锁定的弹道；墙体能挡弹 |
| 补给 | 青色 +35 HP、橙色 +60 弹药；满血不浪费医疗箱，不隔墙拾取 |
| 波次 | 前四波结束后恢复血量和弹药，6 秒后下一波 |
| 第五波 | 清除全部敌人出现 SECTOR SECURED，加入通关奖励 |
| 失败 | 血量归零进入 SYSTEM OFFLINE，不再继续生成/攻击 |
| 重启 | DEPLOY AGAIN 或暂停菜单 RESTART 重置场上敌人、补给、分数与弹药 |
| 暂停 | Esc 停止移动、射击、敌人、计时和换弹，释放鼠标；Resume 正常恢复 |
| 失焦 | 切换到其他窗口会暂停；回来后可继续 |
| 声音 | 射击、换弹、命中等可听见，M 能静音/恢复 |
| HUD | 1280×720、1920×1080、16:10 和超宽窗口下控件不重叠；小地图与敌人血条正确 |
| 最高分 | 返回标题及重新启动游戏后最高分仍然存在 |
| 独立构建 | Windows Build Support 安装后构建成功，完整构建目录可独立启动 |

## 实现边界

- 几何与 HUD 在运行时生成，不依赖外部素材；编辑模式下不会显示完整地图。
- A* 和简单避让面向小型竞技场，尚未在大量单位/复杂地图上进行压力测试。
- 特效使用短生命期 GameObject，没有对象池；最终性能应以 Unity Profiler 为准。
- 生成音效为基础合成音，使用统一的 2D AudioSource；不是完整的空间音频或音乐系统。
- 五波单人模式是当前范围，不包含联网、存档局面和高级动画系统。
